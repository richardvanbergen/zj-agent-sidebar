[package]
name = "viewer"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
serde_json = "1"
crossterm = "0.28"
terminal-colorsaurus = "1.0.3"
fd-lock = "4"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
